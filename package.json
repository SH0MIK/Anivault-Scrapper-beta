{
  "name": "anivault-api",
  "version": "2.0.0",
  "description": "Anime streaming scraper API — AniVault backend (Cloudflare Workers)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.0.0"
  }
}
